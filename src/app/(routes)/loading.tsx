import Box from "@mui/material/Box";
import React from "react";

type Props = {};

export default function Loading({}: Props) {
  return <Box className="flex justify-center items-center h-screen text-blue-500">Loading..</Box>;
}
